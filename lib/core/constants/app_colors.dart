import 'package:flutter/material.dart';

class AppColors {
  
  // why I have two primary colors ? Because why not ?
  static const Color primary = Color(0xFF2B6CEE);

  // accent
  static const Color accent = Color(0xFF9013FE);

  // background coolres :
  static const Color darkBackground = Color(0xFF060608);
  static const Color lightBackground = Color(0xFFF6F6F8);

  // text colors :
  static const Color textDark = Color(0xFFFFFFFF);
  static const Color textLight = Color(0xFF000000);

  // widget cards colors : slightly lighter than bgs
  static const Color cardDark = Color(0xFF191919);
  static const Color cardLight = Color(0xFFFFFFFF);
  
}