import 'package:flutter/material.dart';

class CreateCrewScreen extends StatelessWidget {
  const CreateCrewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Create Crew"));
  }
}
