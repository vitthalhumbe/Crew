import 'package:flutter/material.dart';

class JoinCrewScreen extends StatelessWidget {
  const JoinCrewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Join Crew"));
  }
}
